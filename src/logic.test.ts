import{describe,expect,it}from'vitest';import{backup,conversions,defaultOffers,localRules,quoteTotal,restore}from'./logic';import type{AppData,Lead}from'./types';
const lead=(status:Lead['status'],follow=''):Lead=>({id:status,person:'A',business:'Demo',channel:'Email',url:'',segment:'Auto',location:'Tampa',notes:'',source:'Demo',value:100,lastContact:'',nextFollowUp:follow,status,activity:[]});
describe('revenue logic',()=>{it('calculates funnel conversions',()=>{const result=conversions([lead('Contacted'),lead('Replied'),lead('Quote Sent'),lead('Booked')]);expect(result.response).toBe(.75);expect(result.quote).toBeCloseTo(2/3);expect(result.booking).toBe(.5)});it('prioritizes due replies over new leads',()=>{const sorted=localRules.prioritize([lead('New'),lead('Replied','2020-01-01')],'2026-01-01');expect(sorted[0].status).toBe('Replied')});it('calculates non-negative quote totals',()=>{expect(quoteTotal(500,75)).toBe(575);expect(quoteTotal(-2,-3)).toBe(0)});it('round trips backup data and rejects invalid data',()=>{const data:AppData={leads:[lead('New')],offers:defaultOffers,quotes:[],experimentStart:'2026-01-01'};expect(restore(backup(data)).leads[0].business).toBe('Demo');expect(()=>restore('{}')).toThrow('valid')})});
