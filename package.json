{
  "name": "developer-studio-tampa-site",
  "version": "0.1.0",
  "private": true,
  "description": "Developer Studio Tampa public portfolio website",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "test": "npm run check && node --test scripts/inquiry.test.mjs && npm run build"
  },
  "engines": { "node": ">=18" }
}
